  STRING(REGEX REPLACE ";" "\\\\;" contents "${contents}")
  STRING(REGEX REPLACE "\n" ";" contents "${contents}")
foreach(file ${contents})
    add_subdirectory (${CMAKE_CURRENT_SOURCE_DIR}/${file}/src ${CMAKE_CURRENT_SOURCE_DIR}/${file}/src)
endforeach()
